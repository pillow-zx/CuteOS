#ifndef _CUTEOS_KERNEL_FS_CONTEXT_H
#define _CUTEOS_KERNEL_FS_CONTEXT_H

/*
 * include/kernel/fs_context.h - 挂载上下文预留头文件
 *
 * 为未来的 mount-context API 预留，届时将支持可重配置的文件系统挂载。
 * 当前为空。
 *
 * TODO(vfs): Stage 5 根文件系统挂载先保持硬编码；等支持 mount(2) 或
 * 多挂载点时，再填充 fs_context 结构，否则删除这个占位头文件。
 */

#endif
